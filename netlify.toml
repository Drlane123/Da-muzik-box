# https://docs.netlify.com/configure-builds/file-based-configuration/
[build]
  command = "npm run build"
  publish = "dist"

# Client-side app: any unknown path serves index.html (avoids 404 on refresh / deep links).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
